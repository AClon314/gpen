# Blender Host

Blender v1 integration is a Python add-on that calls the Zig shared library through
`ctypes`. Replacing Blender's built-in Grease Pencil implementation directly is a
later fork-level task, not part of this host scaffold.
